# raw/processed — Archive

Files moved here after successful ingestion. Kept as the audit trail: every vault page's `source_files` frontmatter points back to files in this folder.

Do not re-drop files from here into `raw/inbox/` — the dedup table in `meta/inbox.md` will skip exact re-runs anyway.
