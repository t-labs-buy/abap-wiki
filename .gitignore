# Obsidian per-user session state — changes constantly, never share
.obsidian/workspace.json

# Claude Code per-user local settings — never share (the skills/ folder IS shared)
.claude/settings.local.json

# OS noise
.DS_Store

# Python bytecode (from local runs of the ingest script)
__pycache__/
